name: Build

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  gcc:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install libreadline-dev libsdl2-dev
      - name: Dependency Information
        run: |
          echo "gcc `gcc --version | grep gcc | awk '{print $3}'`"
          echo "sdl2 `sdl2-config --version`"
      - name: Build Test
        run: |
          make
          make debug LEVEL=4
        env:
          CC: gcc
  clang:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install libreadline-dev libsdl2-dev clang
      - name: Dependency Information
        run: |
          echo "clang `clang --version | grep clang | awk '{print $3}'`"
          echo "sdl2 `sdl2-config --version`"
      - name: Build Test
        run: |
          make
          make debug LEVEL=4
        env:
          CC: clang
